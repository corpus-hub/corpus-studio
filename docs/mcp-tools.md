# MCP tool reference

<!-- GENERATED by `npm run docs:mcp` from src/main/ipc/registry. Do not edit. -->

89 tools, plus `health`, which every level can call.
54 read · 27 write · 8 destructive.
A tool above the connection’s permission level is not listed and not callable —
see [docs/mcp.md](./mcp.md) for turning the server on and choosing that level.

Caps applied to arguments before a tool runs: `limit` ≤ 200, `k` ≤ 50,
graph nodes ≤ 300, unresolved references per paper in a
reference tree ≤ 5.
Any response over 4 MiB is truncated at a row boundary and flagged `truncated: true`.

## Contents

- [Projects](#projects) — 3 tool(s)
- [Finding papers](#finding-papers) — 10 tool(s)
- [One paper](#one-paper) — 22 tool(s)
- [Citation graph](#citation-graph) — 5 tool(s)
- [Extraction schemas](#extraction-schemas) — 12 tool(s)
- [Review](#review) — 2 tool(s)
- [Ranking](#ranking) — 4 tool(s)
- [Dossier](#dossier) — 6 tool(s)
- [Queue and jobs](#queue-and-jobs) — 12 tool(s)
- [Export and outlets](#export-and-outlets) — 5 tool(s)
- [Install status](#install-status) — 8 tool(s)

## Projects

A project is one line of enquiry. Most other tools take a `projectId`.

#### `project_get`

One project by its numeric id, or null when no project has that id. Project 0 is the sentinel used for analyses that belong to no project and is not a real project row.

access **read** · channel `projects:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`ProjectDTO | null`

`ProjectDTO` fields: `id: number`, `name: string`, `slug: string`, `description: string | null`, `setup_state: ProjectSetupState`, `goal: string | null`, `questions: string[]`, `category: string | null // e.g. "Enzyme engineering" (design card subtitle)`, `tags: string[] // module/tag pills, e.g. ["Enzyme kinetics"]`, `created_at: string`, `updated_at: string`, `work_count: number // "papers" stat`, `ranked_count: number // works with a computed/overridden relevance (design "ranked")`, `extracted_count: number // works with >=1 extracted fact on a current run`, `failed_count: number // failed processing jobs (design "N failed retrievals" pill)`, `review_count: number // papers with a job parked awaiting a human decision`, `unread_count: number // inclusion_status = 'unread'`, `undecided_count: number // inclusion_status in ('read','uncertain')`, `decided_count: number // inclusion_status in ('included','excluded')`, `processing_count: number`

#### `project_papers_list`

Every paper in a project, with that project’s own interpretation of it — relevance, expansion priority, inclusion status. Those are project-specific: the same paper in another project carries different numbers, and they must never be pooled across projects. Paginated: pass limit and offset (limit defaults to 200); "total" is every paper in the project, so compare it with how many items you received before concluding the project is small.

access **read** · arguments are capped · channel `projects:works`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`ProjectWorkDTO[]`

`ProjectWorkDTO` fields: `work: WorkDTO`, `relevance: number`, `expansion_priority: number`, `inclusion_status: string`, `ranking_explanation: string | null`, `reviewed: number`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `projects_list`

Every project in this install. A project is a research question; papers are interpreted per project, so almost every other tool needs a project_id from here. An empty list means the install has no projects yet, not that the call failed.

access **read** · channel `projects:list`

**Request**

_No arguments._

**Response**

`ProjectDTO[]`

`ProjectDTO` fields: `id: number`, `name: string`, `slug: string`, `description: string | null`, `setup_state: ProjectSetupState`, `goal: string | null`, `questions: string[]`, `category: string | null // e.g. "Enzyme engineering" (design card subtitle)`, `tags: string[] // module/tag pills, e.g. ["Enzyme kinetics"]`, `created_at: string`, `updated_at: string`, `work_count: number // "papers" stat`, `ranked_count: number // works with a computed/overridden relevance (design "ranked")`, `extracted_count: number // works with >=1 extracted fact on a current run`, `failed_count: number // failed processing jobs (design "N failed retrievals" pill)`, `review_count: number // papers with a job parked awaiting a human decision`, `unread_count: number // inclusion_status = 'unread'`, `undecided_count: number // inclusion_status in ('read','uncertain')`, `decided_count: number // inclusion_status in ('included','excluded')`, `processing_count: number`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

## Finding papers

Keyword search, meaning-based search, facets and saved searches.

#### `papers_search`

Search the papers already in this corpus BY WORD, with every filter and sort the app’s own search screen offers. Query syntax: "|" separates ALTERNATIVES ("kcat | turnover" matches either); an empty query means every paper. Matching is a folded (case- and accent-insensitive) substring over title, abstract and venue — it is NOT stemmed, so "binding" does not match "bind". Facet arrays are OR-ed within a facet and AND-ed across facets. year takes an exact year ("1998") or a decade label ("1990–1999", EN DASH). yearFrom/yearTo/minCitations are bounds and apply alongside the facets; a paper with no year never satisfies a year bound. author is a folded substring and a COMMA-SEPARATED list means ANY of them. OMIT projectId for an unscoped search — do NOT pass 0, which is the global-analysis sentinel and is not a project, so it matches nothing. Unscoped, inclusion_status also matches NOTHING (it is a project-specific interpretation of a paper, and the same paper carries a different status in another project) and sort "relevance" silently degrades to TITLE order, because relevance IS the project’s stored ranking. At most 300 rows are matched by the database; limit and offset page over THOSE, and limit defaults to 200 when omitted. "total" is the TRUE match count from a separate uncapped count, so total can exceed what any paging can reach — an offset past the matched rows returns an empty page and says so in scope_note, and the remedy is always to narrow with filters, never to page further. For meaning-based search use papers_search_by_meaning.

access **read** · arguments are capped · channel `search:query`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `query` | `string` | yes | max length 500 |
| `projectId` | `integer \| null` | no | — |
| `filters` | `object \| null` | no | fields below |
| `filters.work_type` | `string[]` | no | max items 200 |
| `filters.venue` | `string[]` | no | max items 200 |
| `filters.year` | `string[]` | no | max items 200 |
| `filters.inclusion_status` | `string[]` | no | max items 200 |
| `filters.content_status` | `string[]` | no | max items 200 |
| `filters.yearFrom` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.yearTo` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.minCitations` | `integer` | no | ≥ 0 |
| `filters.author` | `string` | no | max length 200 |
| `filters.sort` | `"relevance" \| "year" \| "citations" \| "title"` | no | — |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`SearchResultDTO[]`

`SearchResultDTO` fields: `work_id: number`, `title: string`, `year: number | null`, `venue: string | null`, `work_type: string`, `snippet: string | null`, `citation_count: number`, `authors: string[]`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `papers_search_by_meaning`

Search the corpus BY MEANING: the query is embedded and matched against passages of the papers’ extracted text, so it finds wording it does not share. The unit is a PASSAGE, not a paper — each hit carries the verbatim text that matched and its raw cosine score. k is how many passages to ask for, a reading budget and NOT a claim about how many exist; omitting it gives 50. Set workId to search inside ONE paper (this is the by-meaning half of in-paper find); the narrowing happens inside the nearest-neighbour search, so that paper gets its own closest k rather than whatever survived a corpus-wide answer. A non-null "error" is a STATE, not a failure — no model packaged, no active vector space, a dimension mismatch after a model swap — and must be reported as such. Papers with no vector cannot appear in ANY result: read "coverage" before concluding something is absent from the corpus — semantic_coverage_get returns the same numbers with the FULL list of unsearchable papers, which is truncated here.

access **read** · long-running · arguments are capped · channel `search:semantic`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `query` | `string` | yes | max length 500 |
| `projectId` | `integer \| null` | no | — |
| `k` | `integer \| null` | no | — |
| `workId` | `integer \| null` | no | — |

**Response**

`SemanticSearchResultDTO`

`SemanticSearchResultDTO` fields: `hits: SemanticHitDTO[]`, `space_id: number | null`, `strategy: 'index' | 'exhaustive' | null`, `coverage: SemanticCoverageDTO`, `took_ms: number`, `requested_k: number`, `error: string | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `papers_search_count`

How many papers match, counted in SQL and NOT capped — the honest total behind a papers_search result list, which is capped. Takes the same query and filters. Query syntax: "|" separates ALTERNATIVES ("kcat | turnover" matches either); an empty query means every paper. Matching is a folded (case- and accent-insensitive) substring over title, abstract and venue — it is NOT stemmed, so "binding" does not match "bind". Facet arrays are OR-ed within a facet and AND-ed across facets. year takes an exact year ("1998") or a decade label ("1990–1999", EN DASH). yearFrom/yearTo/minCitations are bounds and apply alongside the facets; a paper with no year never satisfies a year bound. author is a folded substring and a COMMA-SEPARATED list means ANY of them. OMIT projectId for an unscoped search — do NOT pass 0, which is the global-analysis sentinel and is not a project, so it matches nothing. Unscoped, inclusion_status also matches NOTHING (it is a project-specific interpretation of a paper, and the same paper carries a different status in another project) and sort "relevance" silently degrades to TITLE order, because relevance IS the project’s stored ranking.

access **read** · channel `search:count`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `query` | `string` | yes | max length 500 |
| `projectId` | `integer \| null` | no | — |
| `filters` | `object \| null` | no | fields below |
| `filters.work_type` | `string[]` | no | max items 200 |
| `filters.venue` | `string[]` | no | max items 200 |
| `filters.year` | `string[]` | no | max items 200 |
| `filters.inclusion_status` | `string[]` | no | max items 200 |
| `filters.content_status` | `string[]` | no | max items 200 |
| `filters.yearFrom` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.yearTo` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.minCitations` | `integer` | no | ≥ 0 |
| `filters.author` | `string` | no | max length 200 |
| `filters.sort` | `"relevance" \| "year" \| "citations" \| "title"` | no | — |

**Response**

`number`

#### `papers_search_facets`

The values you can filter a project’s papers by, with a count each: work_type, venue, year, inclusion_status and content_status. This is how you discover the legal values for papers_search’s facet arrays instead of guessing them — every value returned here can be passed straight back. Year buckets are EXACT YEARS ("1998"); papers_search also accepts a decade label ("1990–1999", EN DASH) which this tool never emits. Each facet’s counts are computed with that facet’s own filter left out, so a count shows what selecting it WOULD add. Requires a real projectId (0 is the global sentinel, not a project). Each dimension is truncated to its 50 largest buckets, with the true bucket count beside them — a corpus can hold thousands of distinct venues.

access **read** · channel `search:facets`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `query` | `string \| null` | no | — |
| `filters` | `object \| null` | no | fields below |
| `filters.work_type` | `string[]` | no | max items 200 |
| `filters.venue` | `string[]` | no | max items 200 |
| `filters.year` | `string[]` | no | max items 200 |
| `filters.inclusion_status` | `string[]` | no | max items 200 |
| `filters.content_status` | `string[]` | no | max items 200 |
| `filters.yearFrom` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.yearTo` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.minCitations` | `integer` | no | ≥ 0 |
| `filters.author` | `string` | no | max length 200 |
| `filters.sort` | `"relevance" \| "year" \| "citations" \| "title"` | no | — |

**Response**

`FacetsDTO`

`FacetsDTO` fields: `work_type: FacetBucketDTO[]`, `venue: FacetBucketDTO[]`, `year: FacetBucketDTO[]`, `inclusion_status: FacetBucketDTO[]`, `content_status: FacetBucketDTO[]`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `papers_stale_list`

The papers in a project whose stored results were produced under inputs that have since changed — an extraction schema edited or newly attached, a different model selected, a reference paper added to the project dossier — with the labels of the stages that would re-run. COMPUTED from the same input fingerprint the scheduler consults when it claims a job, never read from a flag, so it cannot disagree with what a re-run would actually do. A paper that is absent is current; a paper that has never been processed is also absent, because results that were never produced cannot have been produced under stale inputs — read jobs_list for those. This re-runs nothing: call paper_reprocess to act on it. Paginated: pass limit and offset (limit defaults to 200); "total" is every stale paper in the project.

access **read** · arguments are capped · channel `jobs:staleWorks`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 1 |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`StaleWorkDTO[]`

`StaleWorkDTO` fields: `work_id: number`, `stages: string[]`, `stage_ids: string[]`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `papers_with_summaries_list`

Which papers in a project already have a summary, as two lists of work ids: `general` (the paper’s own summary, shared across projects) and `project` (this project’s own reading). Cheap — use it instead of calling paper_summary_get once per paper.

access **read** · channel `summary:have`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`{ general: number[]; project: number[] }`

#### `search_history_list`

The searches saved in this project, most recent first (capped at 25). Each carries the query and its filters as stored JSON, so a past search can be re-run exactly by feeding them back to papers_search. Useful for finding out what the human has already looked for before repeating it.

access **read** · channel `search:listSaved`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`SavedSearchDTO[]`

`SavedSearchDTO` fields: `id: number`, `project_id: number | null`, `name: string`, `query: string`, `filters: string | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `search_record`

Save a search into the project’s history so it can be re-run later. Re-recording an identical query and filters bumps the existing entry rather than duplicating it, and the history is trimmed to the newest 25. "filters" is the SearchFilters object serialized as JSON — serialize it the same way every time or the deduplication will not see the match. This writes to the human’s own history: do not fill it with exploratory queries they did not ask for. If this call times out or your connection drops the write is NOT rolled back — read search_history_list back before retrying.

access **write** · narrower arguments over MCP than in the app · channel `search:record`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `name` | `string` | yes | min length 1, max length 200 |
| `query` | `string` | yes | max length 500 |
| `filters` | `string` | no | max length 4000 |

**Response**

`SavedSearchDTO`

`SavedSearchDTO` fields: `id: number`, `project_id: number | null`, `name: string`, `query: string`, `filters: string | null`

#### `search_web`

Search EXTERNAL literature indexes for papers this corpus does NOT have, then hand a DOI from the result to an import tool to bring one in. Nothing here is in the corpus and nothing is imported by this call. THE QUERY TEXT LEAVES THIS MACHINE — it is sent verbatim to third-party services, so do not put anything from a paper, a note or the user’s own writing into it that they would not publish. Goes over the network, so it is slow and can fail for reasons that have nothing to do with the query. sort "year" is newest-first and "year-asc" oldest-first. filters.sources narrows to papers a given index returned. WHICH indexes are searched depends on what this install has set up, so the values are not enumerable here -- read them off the `sources` field of the rows a first, unfiltered search returns; a paper is kept if ANY listed index returned it, and omitting it searches all of them. It filters what came BACK rather than choosing who is asked, so it makes a search no faster and no more private — every index is queried either way. A row also carries the preprint server it sits on (chemRxiv, Research Square…), which is filterable by the same field. An index that returned nothing for this query yields no rows rather than an error, so an empty result means THIS query, not that the index is absent. page is 1-based and is fetched UPSTREAM, so later pages cost another network round trip. Use it sparingly: a loop over pages is a crawl of somebody else’s index.

access **write** · long-running · arguments are capped · channel `search:web`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `query` | `string` | yes | max length 500 |
| `filters` | `object \| null` | no | fields below |
| `filters.yearFrom` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.yearTo` | `integer` | no | ≥ 1000, ≤ 3000 |
| `filters.minCitations` | `integer` | no | ≥ 0 |
| `filters.author` | `string` | no | max length 200 |
| `filters.sources` | `string[]` | no | max items 32 |
| `filters.sort` | `"relevance" \| "year" \| "year-asc" \| "citations"` | no | — |
| `filters.limit` | `integer` | no | ≥ 1, ≤ 200 |
| `filters.page` | `integer` | no | ≥ 1, ≤ 100 |
| `filters.projectId` | `integer` | no | > 0 |

**Response**

`WebSearchResultDTO[]`

`WebSearchResultDTO` fields: `external_id: string`, `title: string`, `abstract: string`, `authors: string[]`, `year: number | null`, `venue: string | null`, `doi: string | null`, `citation_count: number`, `sources: string[]`, `type: string | null`, `in_project_work_id: number | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `semantic_coverage_get`

What a by-meaning search can and cannot currently see: how many papers are embedded, how many have text at all, how many exist in scope, and which specific papers have no vector and therefore cannot appear in ANY meaning result. Read this before reporting that the corpus contains nothing about a topic. A null "space" means nothing has ever been embedded, which is an honest answer and not an error.

access **read** · channel `search:semanticCoverage`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer \| null` | no | — |
| `workId` | `integer \| null` | no | — |

**Response**

`SemanticCoverageDTO`

`SemanticCoverageDTO` fields: `space: {`, `works_embedded: number`, `works_with_text: number`, `works_total: number`, `chunks: number`, `stale_chunks: number`, `works_stale_only: number`, `indexed: boolean`, `unembedded: { work_id: number; title: string; reason: 'no-text' | 'not-embedded' }[]`, `unembedded_total: number`

## One paper

Everything hanging off a single `workId`. Start with `paper_resolve`.

#### `paper_analyses_list`

Every AI analysis run recorded for this paper in this project, with full provenance: model, provider, prompt and schema version, run timestamp, verifier result and freshness. project_id 0 is the GLOBAL sentinel — analyses belonging to no project. Superseded runs are included and flagged; exactly one run per analysis type is current.

access **read** · channel `works:analyses`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`AnalysisRunDTO[]  (MCP: run yields a ListScope, plus project_context)`

`AnalysisRunDTO` fields: `id: number`, `work_id: number`, `project_id: number // 0 = global`, `analysis_type: string`, `model: string`, `provider: string`, `prompt_version: string`, `schema_version: string`, `run_timestamp: string`, `verifier_result: string | null`, `deterministic_validation: number`, `checks: AnalysisCheckDTO[]`, `supplied_project_context: string | null`, `user_corrections: string | null`, `superseded: number`, `schema_id: number`, `schema_name: string | null`, `doc_input_hash: string | null`, `prompt_input_hash: string | null`, `schema_input_hash: string | null`, `dossier_input_hash: string | null`, `created_at: string`, `run_origin: RunOrigin`, `origin_note: string | null`, `freshness: AnalysisFreshnessDTO`, `facts: FactDTO[]`, `evidence: EvidenceSpanDTO[]`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `paper_attach_pdf`

Attach a PDF to a paper THIS LIBRARY ALREADY HOLDS, by sending its BYTES base64-encoded. Use this — not paper_import_pdf — whenever the paper already has a workId: importing the file as a new paper would leave the library holding the same paper twice. This is the answer to a retrieval that reported refused or skipped: the paper is known, its metadata is right, and only the file was missing. It NEVER changes the title, authors or any other metadata — what an index reported beats what a file is called. A paper that already has a PDF is left ALONE and the reply says so with already_had_file true, because replacing the bytes would invalidate the text, segments and every extracted fact anchored into them. Processing starts immediately — follow the returned jobId with job_get.

access **write** · channel `ingest:attachPdf`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | > 0 |
| `projectId` | `integer` | yes | > 0 |
| `bytesBase64` | `string` | yes | min length 1, max length 89479510 |
| `fileName` | `string` | yes | min length 1, max length 255 |

**Response**

`{ jobId, workId, documentId, alreadyHadFile, ... }`

#### `paper_citation_contexts_get`

Where in this paper each reference is actually cited: the callout sentence, its section, the role the citation plays, and the raw bibliography text as printed. Paginated — pass limit and offset; total is a true COUNT(*), not the page size. Some fields are absent rather than null when the app never analysed them; read paper_citation_outcome_get rather than treating a missing key as "no value found".

access **read** · arguments are capped · channel `works:citationContexts`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`CitationContextDTO[]`

`CitationContextDTO` fields: `id: number`, `raw_bib_text: string | null`, `section: string | null`, `role: string | null`, `resolution_confidence: number | null`, `occurrence_kind: string | null`, `sentence?: string | null`, `para_id?: string | null`, `callout_offset?: number | null`, `page?: number | null`, `target_para_id?: string | null`, `target_sentence?: string | null`, `target_page?: number | null`, `link_verdict?: 'verified' | 'rejected' | 'unverifiable' | 'abstained' | null`, `link_reason?: string | null`, `link_model?: string | null`, `unresolved_reference_id?: number | null`, `target_kind?: 'work' | 'unresolved'`, `target_title?: string | null`, `cited_work_id?: number | null`, `edge_id?: number | null`, `citing_work_id?: number | null`, `document_id?: number | null`, `ordinal?: number | null`, `callout_end?: number | null`, `role_source?: 'rule' | 'llm' | null`, `role_cue?: string | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `paper_citation_outcome_get`

WHY this paper has the citation contexts it has — in particular whether its references were never analysed at all versus analysed and genuinely empty. An empty context list means nothing until you have read this. Returns null when the install predates stage tracking.

access **read** · channel `works:citationOutcome`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`CitationOutcomeDTO | null`

`CitationOutcomeDTO` fields: `status: string`, `note: string | null`

#### `paper_citations_get`

The RESOLVED citation edges touching this paper — references it makes to papers that are in this corpus, and papers in this corpus that cite it. References the app could not match to a corpus paper are NOT here; read paper_unresolved_refs_get for those, or the reference list is silently short. A hub paper can have thousands of edges, so a tool call returns at most 200 of them with the true total beside them.

access **read** · channel `works:citations`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`CitationEdgeDTO[]`

`CitationEdgeDTO` fields: `id: number`, `citing_work_id: number`, `cited_work_id: number`, `edge_type: string`, `citing_title: string`, `cited_title: string`, `contexts: CitationContextDTO[]`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `paper_delete`

Delete a paper and everything derived from it — documents, analyses, facts, measurements, evidence spans, citation edges and queued jobs — from EVERY project, not just one. There is no undo and no archive. Returns false when no work had that id. Ask the human before calling this.

access **destructive** · channel `work:delete`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`boolean`

#### `paper_documents_list`

The concrete documents (PDF versions) of this paper. A WORK is the abstract paper; a DOCUMENT is one stored file of it, and tools that read text take a document_id. Each carries a content status of fulltext, abstract-only or metadata-only — an analysis over an abstract-only document is NOT full-text-backed, however confident it reads — and a text_source of pdf-text-layer, ocr or unknown, where "ocr" means characters a recogniser guessed from pixels and can be wrong in ways that read as plausible. Where the file lives on disk is deliberately not disclosed; "file" reduces to whether a stored file exists and how big it is.

access **read** · channel `works:documents`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`DocumentDTO[]`

`DocumentDTO` fields: `id: number`, `work_id: number`, `version_kind: string`, `title: string | null`, `content_status: string`, `retrieval_status: string`, `is_preferred: number`, `source_url: string | null`, `file: FileLocationDTO | null`, `text_source: 'unknown' | 'pdf-text-layer' | 'ocr'`, `text_confidence: number | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `paper_extract_run`

Run an analysis over ONE paper with the model and store the result with full provenance. READS THE PAPER’S TITLE AND ABSTRACT ONLY — not its full text. If the abstract is empty it falls back to the paper’s extracted paragraphs, and if there is no prose at all it REFUSES rather than analysing a bare title. FOR FULL-TEXT EXTRACTION THIS IS THE WRONG TOOL — full-text extraction is the "schema-extract" pipeline stage, which runs on import and is re-run from the app; there is no tool for it yet. Never report a result from this tool as a full-text extraction. SYNCHRONOUS — it returns the finished run and there is NO job to poll. Pass projectId 0 for an analysis that belongs to no project, or a real project id for one interpreted under that project’s question; project interpretation is never stored globally. THE ANALYSIS TYPES "dossier" AND "ranking" ARE REFUSED AT projectId 0 — both are a project’s own reading and need a real project id. Optional schemaId makes the run extract that schema’s fields. This SUPERSEDES the previous current run for the same paper, project, analysis type and schema and inserts the new one in a single transaction, so there is never more than one current run — but the background pipeline may be extracting the same paper at the same time, and whichever finishes last wins. Check jobs_list for this project first if the pipeline may be running. If this call times out the run may STILL BE RUNNING and the write is NOT rolled back — read paper_analyses_list back rather than retrying.

access **write** · long-running · channel `analysis:run`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |
| `projectId` | `integer` | yes | ≥ 0 |
| `analysisType` | `"extraction" \| "classification" \| "ranking" \| "measurement" \| "relation" \| "dossier"` | yes | — |
| `schemaId` | `integer` | no | ≥ 0 |

**Response**

`AnalysisRunDTO`

`AnalysisRunDTO` fields: `id: number`, `work_id: number`, `project_id: number // 0 = global`, `analysis_type: string`, `model: string`, `provider: string`, `prompt_version: string`, `schema_version: string`, `run_timestamp: string`, `verifier_result: string | null`, `deterministic_validation: number`, `checks: AnalysisCheckDTO[]`, `supplied_project_context: string | null`, `user_corrections: string | null`, `superseded: number`, `schema_id: number`, `schema_name: string | null`, `doc_input_hash: string | null`, `prompt_input_hash: string | null`, `schema_input_hash: string | null`, `dossier_input_hash: string | null`, `created_at: string`, `run_origin: RunOrigin`, `origin_note: string | null`, `freshness: AnalysisFreshnessDTO`, `facts: FactDTO[]`, `evidence: EvidenceSpanDTO[]`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `paper_find_text`

Find a literal string inside ONE paper’s extracted text — the by-word search in a paper. Give exactly one of workId or documentId (a work can hold several documents). Returns each hit with its paragraph index, page and character offsets, so a quotation can be cited back. Matching is a plain substring, not a regex and not stemmed: "eliminase" does not find "elimination". Case-insensitive unless caseSensitive is true. For meaning rather than spelling use papers_search_by_meaning with a workId. Requires the paper’s text to have been extracted; one with only an abstract reports that rather than 0 hits.

access **read** · channel `paper:findText`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | no | > 0 |
| `documentId` | `integer` | no | > 0 |
| `needle` | `string` | yes | min length 1, max length 512 |
| `caseSensitive` | `boolean` | no | — |
| `limit` | `integer` | no | ≥ 1, ≤ 100 |
| `offset` | `integer` | no | ≥ 0 |

**Response**

`TextSearchDTO { state, hits[], total, document_id, ... }  (MCP: plus project_context)`

`TextSearchDTO` fields: `state: DocumentTextState`, `document_id: number | null`, `stage_run_id: number | null`, `content_status: string | null`, `hits: TextHitDTO[]`, `total: number`, `truncated: boolean`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `paper_get`

One paper by its numeric work_id: title, abstract, year, venue, authors, identifiers and content status. Call paper_resolve first if all you have is a DOI or a title. Returns null when no work has that id. A WORK is the abstract output; the concrete PDFs it has are DOCUMENTS, listed by paper_documents_list.

access **read** · channel `works:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`WorkDetailDTO | null  (MCP: plus project_context, see below)`

`WorkDetailDTO` fields: `authors: AuthorDTO[]`, `identifiers: IdentifierDTO[]`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `paper_import`

Add a paper to a project by IDENTIFIER: doi, pmid, arxiv, url, or a title to look up. The identifier is resolved against the academic indexes FIRST, so the paper lands with real metadata (authors, year, venue, abstract) and the reply names the paper the identifier turned out to be — check it, an identifier that resolves is not always the one you meant. An identifier no index recognises is REFUSED rather than stored as a placeholder. Importing the same paper twice updates the existing record instead of duplicating it. Processing starts immediately; follow the returned jobId with job_get. To add a PDF you already hold, use paper_import_pdf — this tool never takes a file path.

access **write** · long-running · narrower arguments over MCP than in the app · channel `ingest:run`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | > 0 |
| `kind` | `"doi" \| "pmid" \| "arxiv" \| "title" \| "url"` | yes | — |
| `value` | `string` | yes | min length 1, max length 2000 |

**Response**

`{ jobId, workId?, resolvedTitle? }`

#### `paper_import_pdf`

Add a PDF you already have to a project, by sending its BYTES base64-encoded. Use this rather than a path — no tool here reads the filesystem. The bytes must really be a PDF (checked by magic bytes) and at most 64 MB. fileName names the stored file, reduced to plain characters inside the library folder — it cannot point anywhere else. Re-sending identical bytes under the same name reuses the one copy rather than duplicating it. Text extraction and analysis start immediately — follow the returned jobId with job_get.

access **write** · channel `ingest:pdfBytes`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | > 0 |
| `bytesBase64` | `string` | yes | min length 1, max length 89479510 |
| `fileName` | `string` | yes | min length 1, max length 255 |
| `title` | `string` | no | max length 1000 |

**Response**

`{ jobId, workId, documentId, ... }`

#### `paper_remove_from_project`

Take a paper OUT OF ONE PROJECT, leaving the paper and every other project that holds it untouched. This is the one to use when a paper does not belong in a collection — paper_delete erases it everywhere and cannot be undone. Removes this project’s own reading of the paper (relevance, inclusion status, reference flag); analyses are keyed by paper AND project and survive, so re-adding finds them again. Returns false when the paper was not in that project.

access **write** · channel `work:removeFromProject`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`boolean`

#### `paper_reprocess`

Re-run ONE paper through its WHOLE processing pipeline — every stage stages_list reports, from fetching the file through text extraction and segmentation to extraction against each attached schema. READ "state" FIRST, NOT THE JOB IDS. With force false (the default) this plans against the stage cache: every stage whose inputs are unchanged is settled straight back to done WITHOUT running, so you get a full list of job ids and nothing is actually re-analysed — that is state "already-current", and the way to get a re-run you can rely on is to call again with force true. force true discards the paper’s current stage runs first, so there is no cache left to hit; superseded_run_ids then names exactly what was thrown away, and an empty superseded_run_ids always means nothing was. projectId is the project you are acting for and must be a real project id: 0 is the sentinel for "belongs to no project" and is not something a re-run can be performed on behalf of. NOTE THAT force true IS PAPER-WIDE, NOT PROJECT-WIDE: it discards every current run of this paper, including analyses another project made of it that your project never touched. They are re-planned, so nothing is lost permanently, but that project pays for the re-analysis. Use paper_stage_rerun when only one step is wrong. WHEN IT DISCARDS ANYTHING IT DISCARDS IT FOR GOOD: the superseded runs’ extracted paragraphs, citation contexts and embeddings are DELETED and their facts marked superseded. They are not archived and there is no undo — the only way back is to let the pipeline produce them again. It also cascades DOWNSTREAM AND ACROSS PROJECTS: any other project that consumed the same output has its work invalidated and re-planned too, and planned_project_ids says which. ASYNCHRONOUS: it returns job ids, not results; poll job_get. The write is NOT rolled back if this call times out or the connection drops — read the state back with jobs_list rather than retrying, because a retry would discard the replacement too.

access **destructive** · long-running · channel `jobs:reprocessWork`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | > 0 |
| `projectId` | `integer` | yes | ≥ 1 |
| `force` | `boolean \| null` | no | — |

**Response**

`RerunResultDTO`

`RerunResultDTO` fields: `state: RerunState`, `note: string`, `superseded_run_ids: number[]`, `stale_run_ids: number[]`, `created_job_ids: number[]`, `requeued_job_ids: number[]`, `pending_job_ids: number[]`, `all_job_ids: number[]`, `planned_project_ids: number[]`, `queue_running: boolean`

#### `paper_resolve`

Turn a DOI, arXiv id, PMID or title into the work_id every other paper tool needs. START HERE when all you have is an identifier or a name — no other tool accepts one. This searches only what is ALREADY in this corpus; it fetches nothing and imports nothing, so a paper that exists in the world but not here comes back "not-found". READ "state" BEFORE ANYTHING ELSE, because only one of the four carries a work_id: "resolved" has it and says what it matched on; "ambiguous" means several papers fit and you must choose between the candidates rather than guess, with "more" true when even more matched than are listed; "not-found" carries near misses as suggestions, which are NOT matches; "invalid" means the reference itself could not be read. kind narrows the interpretation and defaults to "auto", which infers it.

access **read** · channel `paper:resolve`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `paperRef` | `string` | yes | min length 1, max length 500 |
| `kind` | `"doi" \| "arxiv" \| "pmid" \| "title" \| "auto" \| null` | no | — |

**Response**

`ResolveWorkDTO`

#### `paper_stage_rerun`

Re-run ONE named pipeline stage for one paper, discarding what it produced. Use this when a specific step is wrong — a bad text extraction, a schema extraction to redo against the paper’s full text — rather than reprocessing the whole paper. Call stages_list for the stage ids. The named stage FANS OUT where the pipeline does: "schema-extract" has one run per attached extraction schema and ALL of them are re-run, because re-running one and leaving the rest stale would report success over a half-updated paper. Stages that run over the whole corpus rather than a paper are not accepted here; they belong to no paper. READ "state": "rerunning" means output was discarded and will be produced again, "no-current-runs" means the stage has no current run for this paper so nothing happened, and "already-queued" means the work was outstanding already. projectId is the project you are acting for and must be a real project id, because the invalidation is re-planned per project and the caller must say which one it is acting for; work-scoped stages store the 0 sentinel and are still matched. WHEN IT DISCARDS ANYTHING IT DISCARDS IT FOR GOOD: the superseded runs’ extracted paragraphs, citation contexts and embeddings are DELETED and their facts marked superseded. They are not archived and there is no undo — the only way back is to let the pipeline produce them again. It also cascades DOWNSTREAM AND ACROSS PROJECTS: any other project that consumed the same output has its work invalidated and re-planned too, and planned_project_ids says which. ASYNCHRONOUS: it returns job ids, not results; poll job_get. The write is NOT rolled back if this call times out or the connection drops — read the state back with jobs_list rather than retrying, because a retry would discard the replacement too.

access **destructive** · long-running · channel `jobs:rerunStage`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | > 0 |
| `stage` | `"retrieve" \| "download" \| "optimize" \| "extract-text" \| "zotero-push" \| "ocr" \| "segment" \| "embed" \| "summarise" \| "references" \| "citation-contexts" \| "schema-extract" \| "review-records" \| "reference-abstracts"` | yes | — |
| `projectId` | `integer` | yes | ≥ 1 |

**Response**

`RerunResultDTO`

`RerunResultDTO` fields: `state: RerunState`, `note: string`, `superseded_run_ids: number[]`, `stale_run_ids: number[]`, `created_job_ids: number[]`, `requeued_job_ids: number[]`, `pending_job_ids: number[]`, `all_job_ids: number[]`, `planned_project_ids: number[]`, `queue_running: boolean`

#### `paper_stages_rerun`

Re-run SEVERAL named stages of one paper in a single transaction, discarding what each produced. This is what papers_stale_list is for: it reports the stages whose inputs changed, and this re-runs exactly those. Prefer it to paper_reprocess with force, which discards EVERY current run of the paper — including fetching the file and reading its text again — when only a later stage went stale. The cascade still reaches everything genuinely downstream of the stages named, so nothing is left half-updated. Each stage fans out where the pipeline does. One call rather than several: two calls let something else re-resolve a key in between, and the second would destroy what the first created. WHEN IT DISCARDS ANYTHING IT DISCARDS IT FOR GOOD: the superseded runs’ extracted paragraphs, citation contexts and embeddings are DELETED and their facts marked superseded. They are not archived and there is no undo — the only way back is to let the pipeline produce them again. It also cascades DOWNSTREAM AND ACROSS PROJECTS: any other project that consumed the same output has its work invalidated and re-planned too, and planned_project_ids says which. ASYNCHRONOUS: it returns job ids, not results; poll job_get. The write is NOT rolled back if this call times out or the connection drops — read the state back with jobs_list rather than retrying, because a retry would discard the replacement too.

access **destructive** · long-running · channel `jobs:rerunStages`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | > 0 |
| `stages` | `"retrieve" \| "download" \| "optimize" \| "extract-text" \| "zotero-push" \| "ocr" \| "segment" \| "embed" \| "summarise" \| "references" \| "citation-contexts" \| "schema-extract" \| "review-records" \| "reference-abstracts"[]` | yes | min items 1 |
| `projectId` | `integer` | yes | ≥ 1 |

**Response**

`RerunResultDTO`

`RerunResultDTO` fields: `state: RerunState`, `note: string`, `superseded_run_ids: number[]`, `stale_run_ids: number[]`, `created_job_ids: number[]`, `requeued_job_ids: number[]`, `pending_job_ids: number[]`, `all_job_ids: number[]`, `planned_project_ids: number[]`, `queue_running: boolean`

#### `paper_summary_generate`

Write a paper’s summary with the model, and store it with full provenance. SYNCHRONOUS: it returns when the summary is finished and there is NO job to poll. kind "general" summarises the paper on its own terms and is stored globally (projectId 0); kind "project" summarises it against one project’s question, REQUIRES a real projectId greater than 0, and REQUIRES that project’s topic dossier to have been built first — without one it returns state "no-dossier", which is an answer and not a failure: call dossier_build and try again. A paper with no readable text returns state "no-source"; ingest its PDF or add an abstract. This SUPERSEDES the previous current summary of the same kind and inserts the new one in a single transaction, so there is never more than one current summary per paper per project. If this call times out or your connection drops the write is NOT rolled back and may still be running — read the state back with paper_summary_get before retrying.

access **write** · long-running · channel `summary:generate`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |
| `projectId` | `integer` | yes | ≥ 0 |
| `kind` | `"general" \| "project"` | yes | — |

**Response**

`WorkSummaryDTO`

`WorkSummaryDTO` fields: `kind: SummaryKind`, `work_id: number`, `project_id: number`, `body: string | null`, `source_scope: string | null`, `run: AnalysisRunDTO | null`

#### `paper_summary_get`

Read a paper’s stored prose summary. TWO DIFFERENT THINGS LIVE BEHIND THIS ONE TOOL and you must choose: kind "general" is the paper’s own account of itself, shared by every project (pass projectId 0); kind "project" is ONE project’s reading of the paper in light of its own question, and REQUIRES a real projectId greater than 0 — kind "project" with projectId 0 is refused, because a project’s framing must never be served to other projects as the paper’s own claim. Never quote a project summary as what the paper says. `state` tells you what you got: "ready" (there is prose), "missing" (nothing written yet — call paper_summary_generate), "failed", "no-source" (the paper has no readable text) or "no-dossier" (a project summary needs a built dossier first). `run` carries the model, prompt version and timestamp behind the prose.

access **read** · channel `summary:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |
| `projectId` | `integer` | yes | ≥ 0 |
| `kind` | `"general" \| "project"` | yes | — |

**Response**

`WorkSummaryDTO  (MCP: plus project_context when the project has background)`

`WorkSummaryDTO` fields: `kind: SummaryKind`, `work_id: number`, `project_id: number`, `body: string | null`, `source_scope: string | null`, `run: AnalysisRunDTO | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `paper_text_get`

Read a paper’s extracted text. Give exactly one of workId or documentId. Paged, because a paper is far larger than one response: pass page for a specific page, or fromIdx/toIdx for a paragraph range, and the reply says how much is left. Returns the text layer the analyses were actually run against, not the PDF — so it is the right thing to quote when checking what a stage could have seen.

access **read** · channel `paper:text`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | no | > 0 |
| `documentId` | `integer` | no | > 0 |
| `fromIdx` | `integer` | no | ≥ 0 |
| `toIdx` | `integer` | no | ≥ 0 |
| `page` | `integer` | no | ≥ 1 |

**Response**

`DocumentTextDTO { state, paragraphs[], from_idx, to_idx, total, ... }  (MCP: plus project_context)`

`DocumentTextDTO` fields: `state: DocumentTextState`, `document_id: number | null`, `stage_run_id: number | null`, `content_status: string | null`, `paragraphs: DocumentParagraphDTO[]`, `total_paragraphs: number`, `truncated: boolean`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `paper_unresolved_refs_get`

References this paper makes that the app parsed but could NOT match to a paper in the corpus, with their raw bibliography text and any guessed DOI, title, venue or year. These are never silently dropped; use unresolved_ref_resolve to attach one to a work, or references_retrieve to go and fetch its metadata. References naming the same paper from different bibliographies are merged, so one entry can stand for several. A review can cite hundreds, so a tool call returns at most 200 with the true total.

access **read** · channel `works:unresolved`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`UnresolvedReferenceDTO[]`

`UnresolvedReferenceDTO` fields: `id: number`, `citing_work_id: number`, `raw_bib_text: string`, `guessed_doi: string | null`, `guessed_title: string | null`, `section: string | null`, `status: string`, `retrieval_status: ReferenceRetrievalStatus`, `retrieval_error: string | null`, `retrieval_kind: ReferenceRetrievalKind | null`, `relevance: number | null`, `scored_on: string | null`, `abstract_state: ReferenceAbstractStateDTO`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `unresolved_ref_resolve`

Attach one unresolved reference to a paper ALREADY IN THIS CORPUS, creating the citation edge. Find the target with paper_resolve or papers_search first and pass its work_id. edgeType defaults to "cites". This is a permanent claim that paper A cites paper B — do not guess: if the raw bibliography text is ambiguous, leave it unresolved. If this call times out or your connection drops the write is NOT rolled back; read the state back with paper_unresolved_refs_get before retrying.

access **write** · narrower arguments over MCP than in the app · channel `works:resolveRef`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `unresolvedId` | `integer` | yes | ≥ 0 |
| `target` | `{ workId: integer }` | yes | — |
| `edgeType` | `"cites" \| "extends" \| "contradicts" \| "uses-method" \| "reviews" \| "related"` | no | — |

**Response**

`ResolveReferenceResultDTO`

`ResolveReferenceResultDTO` fields: `edgeId: number | null`, `citingWorkId: number`, `citedWorkId: number`, `createdWork: boolean // true when a new work was inserted`, `matchedBy: 'existing-id' | 'doi' | 'normalized-title' | 'created'`, `contextsMoved: number`, `contextsDiscardedSelfCite: number`

## Citation graph

Who cites whom, and pulling in the references of a paper you already have.

#### `graph_get`

The citation graph of a project: papers as nodes with their relevance, and the resolved citation edges between them. Only edges BETWEEN papers in this corpus appear — a paper’s references to work the corpus does not hold are not edges here, so this is not a picture of what the papers cite, it is a picture of what they cite that you also have. limit is the node budget (clamped to 300 for tools; the response also carries the true total) and minRelevance drops nodes below a 0..1 relevance floor. For what a paper cites that is MISSING, use reference_tree_get.

access **read** · arguments are capped · channel `graph:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `opts` | `{ limit?: integer, minRelevance?: number } \| null` | no | — |

**Response**

`GraphDTO`

`GraphDTO` fields: `nodes: GraphNodeDTO[]`, `edges: GraphEdgeDTO[]`, `total_works: number`, `shown_works: number`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `reference_abstract_get`

The abstract an index returned for ONE unresolved reference — a paper this corpus cites but does not hold — together with the title the index matched, its DOI and which index answered. READ "outcome" BEFORE "abstract", because the four non-"found" values mean different things and only one of them is about the paper: "absent" is an index that answered and holds no abstract; "unreachable" means the index could not be reached and says NOTHING about whether an abstract exists; "ambiguous" means several records fit the printed reference and none was trusted; "nothing-to-ask-with" means the entry prints no DOI and no usable title. A null outcome means nothing has looked yet. matched_title is a CLAIM that this reference is that paper — compare it with the reference before relying on the text. Returns null only when no such unresolved id exists.

access **read** · channel `graph:referenceAbstract`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `unresolvedId` | `integer` | yes | ≥ 0 |

**Response**

`ReferenceAbstractDTO | null`

`ReferenceAbstractDTO` fields: `unresolved_id: number`, `abstract: string | null`, `matched_title: string | null`, `doi: string | null`, `source: 'openalex' | 'crossref' | null`, `outcome: ReferenceAbstractOutcome | null`, `fetched_at: string | null`

#### `reference_retrievals_get`

The retrieval status of specific unresolved references — whether a fetch was attempted, succeeded, or failed and why. This is how you follow up a references_retrieve call per reference, rather than inferring it from the queue. Pass the unresolved ids you submitted; a tool call reports on at most 200 of them per call and says in scope_note how many ids it dropped, so ask again for the rest rather than assuming the unreported ones have no retrieval.

access **read** · arguments are capped · channel `graph:referenceRetrievals`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `unresolvedIds` | `integer[]` | yes | max items 2000 |

**Response**

`ReferenceRetrievalDTO[]`

`ReferenceRetrievalDTO` fields: `unresolved_id: number`, `retrieval_status: ReferenceRetrievalStatus`, `retrieval_error: string | null`, `job_id: number | null`, `work_id: number | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `reference_tree_get`

The project’s papers together with the references they make that are NOT in the corpus — the frontier of what you are missing. unresolvedPerWork is how many unmatched references to return per paper (0 = none, the default; a tool call is capped at 5, and paper_unresolved_refs_get gives you all of one paper’s). Each carries an unresolved id you can feed to references_retrieve. Node budget clamped to 300 for tools; "total_works" and "total_unresolved" are the true counts, so compare them with what you received rather than assuming you saw everything.

access **read** · arguments are capped · channel `graph:referenceTree`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `opts` | `{ limit?: integer, unresolvedPerWork?: integer } \| null` | no | — |

**Response**

`ReferenceTreeDTO`

`ReferenceTreeDTO` fields: `nodes: ReferenceTreeNodeDTO[]`, `edges: GraphEdgeDTO[]`, `total_works: number`, `shown_works: number`, `unresolved: UnresolvedReferenceNodeDTO[]`, `total_unresolved: number`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `references_retrieve`

Go and FETCH the real metadata for unresolved references from the external indexes, and queue the resulting papers for processing. Takes up to 200 unresolved ids from paper_unresolved_refs_get or reference_tree_get. This is ASYNCHRONOUS: it returns immediately with what it queued, and you poll jobs_list or job_get for the outcome — do not treat the return value as the finished import. It goes over the network and it ADDS PAPERS to the corpus. Retrieval progress is readable with reference_retrievals_get. If this call times out the queued work is NOT rolled back.

access **write** · long-running · channel `graph:retrieveRefs`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `unresolvedIds` | `integer[]` | yes | min items 1, max items 200 |

**Response**

`RetrieveReferencesResultDTO`

`RetrieveReferencesResultDTO` fields: `queued: Array<{ unresolved_id: number; job_id: number; work_id: number }>`, `skipped: Array<{ unresolved_id: number; reason: 'not-retrievable' | 'already-retrieving' }>`

## Extraction schemas

The field sets the model fills in per paper, and how much of the corpus they cover.

#### `extraction_rows_get`

The values extracted from a project’s papers: quantity, number, unit, the conditions it was measured under, the fact kind, and the evidence span quoted from the paper. Pass `workId` to restrict to one paper. ONE MEASURED VALUE CAN LEGITIMATELY APPEAR TWICE, under two different schemas that both asked for it — that is not duplication and must not be deduplicated; the schema linkage on each row says which reading it is. `fact_kind` says how the value was arrived at (directly-reported, inferred, supplied-by-project-context, uncertain-conflicting) and only "directly-reported" means the paper stated it. Each row carries the evidence span it came from, and a span with `verbatim: false` means the model asserted that wording and it could NOT be found in the document — never quote such a span as something the paper says. Paginated: `total` counts the whole project, or just the one paper when you pass `workId`.

access **read** · arguments are capped · channel `extraction:rows`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer \| null` | no | — |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`ExtractionRowDTO[]  (MCP: a ListScope, plus project_context for a one-paper read)`

`ExtractionRowDTO` fields: `row_key: string`, `measurement_id: number | null`, `fact_id: number`, `work_id: number`, `work_title: string`, `quantity: string`, `value_num: number | null`, `value_text: string | null`, `unit: string | null`, `conditions: string | null`, `subject: string | null`, `fact_kind: string`, `fold: FoldImprovementDTO | null`, `evidence: EvidenceSpanDTO | null`, `status: 'validated' | 'review' | 'conflict' | 'invalid' // derived`, `run_origin: RunOrigin`, `origin_note: string | null`, `field_id: number | null`, `field_key: string | null`, `field_label: string | null`, `field_unit: string | null`, `field_type: ExtractionFieldType | null`, `field_sort_order: number | null`, `schema_id: number | null`, `schema_key: string | null`, `schema_name: string | null`, `run_schema_id: number | null`, `run_schema_name: string | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `extraction_status_get`

Counts describing a project’s extraction and review state: how many values were auto-validated, how many are awaiting human judgement, how many are structurally invalid or in conflict, and the quality-control sample. Read this BEFORE review_queue_get so you know the queue’s true size rather than inferring it from a page.

access **read** · channel `extraction:summary`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionStatusSummaryDTO`

`ExtractionStatusSummaryDTO` fields: `total_records: number`, `auto_validated: number`, `needs_interpretation: number // status 'review' (partial / assumed etc.)`, `conflicting: number // status 'conflict'`, `structurally_invalid: number // status 'invalid'`, `qc_sample: ExtractionQcSampleDTO[] // random sample of auto-validated records`

#### `project_schema_attach`

Apply a global extraction schema to a project, so its Extraction view and its future extraction runs use it. Does not extract anything by itself. Returns the project’s attached schemas. If this call times out or your connection drops the write is NOT rolled back — read the state back before retrying.

access **write** · channel `projectSchemas:attach`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `schemaId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionSchemaDTO[]`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `project_schema_detach`

Stop a project applying an extraction schema. DETACHING IS NOT DELETING: the schema definition survives, and every value already extracted under it in this project survives and stays readable. This is the reversible way to remove a schema from a project. Returns the project’s remaining attached schemas.If this call times out or your connection drops the write is NOT rolled back — read the state back before retrying.

access **write** · channel `projectSchemas:detach`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `schemaId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionSchemaDTO[]`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `project_schemas_list`

The extraction schemas a project applies — the subset of the global schema list its Extraction view uses. A schema missing from here still exists and still holds its data; it is simply not applied in this project.

access **read** · channel `projectSchemas:list`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionSchemaDTO[]`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `schema_coverage_get`

How much of a project each attached extraction schema actually covers: how many papers have values under it and how many do not. Use it to tell "this schema found nothing" from "this schema was never run on these papers".

access **read** · channel `projectSchemas:coverage`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`SchemaCoverageDTO[]`

`SchemaCoverageDTO` fields: `schema_id: number`, `works_total: number`, `works_with_values: number`, `works_without_values: number`, `measurement_count: number`

#### `schema_delete`

DELETE an extraction schema and all of its field definitions. Measurements already extracted under it SURVIVE but lose the field that gave them meaning, and there is no way to reattach them — the definition a scientist wrote is gone and must be retyped. To stop a project applying a schema WITHOUT destroying anything, use project_schema_detach instead. Returns the remaining schemas.If this call times out or your connection drops the write is NOT rolled back — read the state back before retrying.

access **destructive** · channel `schemas:delete`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `schemaId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionSchemaDTO[]`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `schema_export`

One extraction schema as a portable bundle: its name, description and every field definition, with no extracted data and no ids. This is the shareable form — hand it to a colleague, who imports it in their own copy of the app.

access **read** · channel `schemas:export`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `schemaId` | `integer` | yes | ≥ 0 |

**Response**

`SchemaBundleDTO`

`SchemaBundleDTO` fields: `format: number`, `name: string`, `description: string | null`, `fields: SchemaBundleFieldDTO[]`

#### `schema_field_delete`

DELETE one field from an extraction schema. The field definition is gone and must be retyped by hand; values already extracted into it survive but no longer have a field describing them. An extraction already running keeps the old field list and will write into a field that no longer exists. Returns the updated schema.If this call times out or your connection drops the write is NOT rolled back — read the state back before retrying.

access **destructive** · channel `schemas:deleteField`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `fieldId` | `integer` | yes | ≥ 0 |

**Response**

`ExtractionSchemaDTO`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `schema_fields_reorder`

Set the order the fields of an extraction schema are read in — the column order in the Extraction table and the line order in a paper’s readings. PRESENTATION ONLY: no extracted value changes, the schema version does not move, and nothing already extracted becomes stale. `fieldIds` must name EVERY field of the schema exactly once; a partial list is refused rather than appended to, because the fields left out would land at positions nobody chose. Returns the updated schema with its fields in the new order.

access **write** · channel `schemas:reorderFields`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `schemaId` | `integer` | yes | ≥ 0 |
| `fieldIds` | `integer[]` | yes | max items 200 |

**Response**

`ExtractionSchemaDTO`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

#### `schema_presets_list`

The premade extraction schemas this app ships with, as importable bundles. Nothing is read from the database and nothing is created — a person imports one in the app to make it real. Use this to see what vocabulary this app already has for a field before assuming none exists.

access **read** · channel `schemas:presets`

**Request**

_No arguments._

**Response**

`SchemaBundleDTO[]`

`SchemaBundleDTO` fields: `format: number`, `name: string`, `description: string | null`, `fields: SchemaBundleFieldDTO[]`

#### `schemas_list`

Every extraction schema defined in this install — the user’s definitions of WHAT to pull out of papers, with each schema’s fields (label, data type, unit, whether required, allowed values). Schemas are global, not per project; which ones a project applies is project_schemas_list. Read this before extraction_rows_get so you know what the columns mean.

access **read** · channel `schemas:list`

**Request**

_No arguments._

**Response**

`ExtractionSchemaDTO[]`

`ExtractionSchemaDTO` fields: `id: number`, `key: string`, `name: string`, `description: string | null`, `version: string`, `is_builtin: boolean`, `export_alias: string | null`, `fields: ExtractionFieldDTO[]`, `measurement_count: number`, `attached_project_count: number`

## Review

The human verdict queue over extracted facts.

#### `review_queue_get`

The facts in a project awaiting human judgement: what was extracted, why it was escalated (`reason`, `failed_checks`), the verdict already recorded if any, and that fact’s verdict history. THIS QUEUE IS A SAMPLE-TOPPED LIST, NOT A PAGE-ABLE ONE — it folds in a random quality-control sample of auto-validated records, so the population differs between two calls and there is deliberately no offset. To see more, ask for a larger `limit` and re-read; do not try to page. `total` is the true size. A verdict marked `stale: true` was passed on an EARLIER extraction run, so the item stays in the queue. Use extraction_status_get for the queue’s shape before reading it.

access **read** · arguments are capped · channel `review:queue`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `limit` | `integer \| null` | no | — |

**Response**

`ReviewItemDTO[]`

`ReviewItemDTO` fields: `fact_id: number`, `work_id: number`, `work_title: string`, `kind: string // uncertain-conflicting etc.`, `predicate: string`, `value_text: string | null`, `verifier_result: string | null`, `failed_checks: AnalysisCheckDTO[]`, `reason: string // why it needs review`, `verdict: FactVerdictDTO | null`, `verdict_history: FactVerdictDTO[]`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `review_record_verdict`

Record a human judgement on one extracted fact: accept it, correct its value, reject it, or return it to undecided. `correctedValue` is REQUIRED when verdict is "corrected" and REFUSED for every other verdict — that rule is enforced but cannot be expressed in this schema, so read it here. `projectId` must be a real project, never 0: a verdict is never global. This is APPEND-ONLY — it never alters the fact, its evidence or the analysis run that produced it, and a verdict is retracted by recording "unresolved", not by deleting. ATTRIBUTION IS RESOLVED BY THE APP, NOT BY YOU: the verdict is signed with the operating-system account of the person who owns this install, marked as having come through the agent connection. You are signing scientific judgement in their name — do not record a verdict you were not asked for. Get `factId`s from review_queue_get. If this call times out or your connection drops the write is NOT rolled back.

access **write** · channel `review:verdict`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | > 0 |
| `factId` | `integer` | yes | > 0 |
| `verdict` | `"accepted" \| "corrected" \| "rejected" \| "unresolved"` | yes | — |
| `correctedValue` | `string` | no | min length 1, max length 2000 |
| `note` | `string` | no | max length 2000 |

**Response**

`FactVerdictDTO`

`FactVerdictDTO` fields: `id: number`, `fact_id: number`, `project_id: number`, `verdict: FactVerdictKind`, `corrected_value: string | null`, `note: string | null`, `reviewer: string`, `created_at: string`, `stale: boolean`, `analysis_run_id: number`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

## Ranking

Two separate scores per paper, plus inclusion decisions.

#### `ranking_get`

Both of a project’s rankings for every paper in it, with the stored explanation for each and any human override. RELEVANCE and EXPANSION_PRIORITY MEASURE DIFFERENT THINGS — relevance is how much the paper bears on the question, expansion_priority is how worthwhile following its citations would be. Never average, blend or otherwise combine them; report them separately. `ranking_explanation` is the app’s own stored reason for the score and `user_overrides` is a JSON string of the values a human set by hand, which take precedence over the computed ones. These numbers belong to THIS project: the same paper in another project has different ones. Paginated — read `total`.

access **read** · arguments are capped · channel `ranking:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `sortBy` | `"relevance" \| "expansion" \| "year" \| "citations" \| null` | no | — |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`RankingRowDTO[]`

`RankingRowDTO` fields: `work_id: number`, `title: string`, `year: number | null`, `work_type: string`, `relevance: number | null`, `relevance_rank: number | null`, `expansion_priority: number | null`, `expansion_rank: number | null`, `inclusion_status: string`, `is_reference: boolean`, `scored_on: string | null`, `ranking_explanation: string | null`, `reviewed: number`, `user_overrides: string | null // JSON: {relevance?, expansion_priority?, reason?}`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `ranking_override_score`

Set one of the two scores for one paper in one project BY HAND, overriding the computed value, with an optional reason. `field` is exactly one of relevance or expansion_priority — they are separate measures and setting one says nothing about the other. The override is recorded in the row’s `user_overrides` JSON and SURVIVES every later scoring pass, which leaves an overridden column exactly as you set it. It does NOT rewrite the stored `ranking_explanation`, so until the next scoring pass the explanation still describes the measured number rather than yours. Returns nothing; read it back with ranking_get. If this call times out or your connection drops the write is NOT rolled back.

access **write** · narrower arguments over MCP than in the app · channel `ranking:override`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |
| `field` | `"relevance" \| "expansion_priority"` | yes | — |
| `value` | `number` | yes | ≥ 0, ≤ 1 |
| `reason` | `string \| null` | no | — |

**Response**

`void`

#### `ranking_recompute`

Ask for both rankings to be scored again, for EVERY project rather than only this one: the scoring pass is a single sweep over the whole library, and `projectId` names who is asking, not what is rescored. IT RETURNS BEFORE THE SCORES CHANGE — the work is queued and a local model reads each paper’s title and abstract against its project’s description, which takes about a tenth of a second per paper. Read the numbers back with ranking_get afterwards, and do not treat the ones you read immediately as the new ones. HUMAN OVERRIDES ARE RESPECTED: a score a person set by hand is left exactly as they set it. You RARELY need this — editing a project’s description, importing a paper and resolving a reference each re-score the library on their own. Call it when you want the same inputs scored again.

access **write** · channel `ranking:recompute`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`{ queued: boolean, discardedRuns: number }`

#### `ranking_set_inclusion`

Record whether a paper is included in, excluded from, or still undecided for a project, with an optional free-text reason. This is the scientist’s editorial decision about THIS project and is stored on the project’s own row — it does not change the paper, and it is invisible to every other project. Returns nothing; read it back with ranking_get. If this call times out or your connection drops the write is NOT rolled back — read the state back before retrying.

access **write** · narrower arguments over MCP than in the app · channel `ranking:setInclusion`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |
| `status` | `"read" \| "unread" \| "included" \| "excluded" \| "uncertain"` | yes | — |
| `reason` | `string \| null` | no | — |

**Response**

`void`

## Dossier

The project-level synthesis built from papers marked as references.

#### `dossier_add_paper`

Mark (or unmark) a paper as a REFERENCE PAPER for a project — one of the papers the topic dossier is built from. Same action as "mark as reference paper" on the Ranking screen. Pass isReference: false to unmark. THIS DOES NOT REBUILD THE DOSSIER: the paper appears immediately in dossier_status_get’s `references` but not in its `sources`, and no claim of it enters dossier_get, until you call dossier_build. Stopping here is the commonest way this sequence is left half-done. If this call times out or your connection drops the write is NOT rolled back.

access **write** · channel `ranking:markReference`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |
| `isReference` | `boolean` | yes | — |

**Response**

`RankingRowDTO[]  (MCP: { paper, is_reference, references_total, next_step })`

`RankingRowDTO` fields: `work_id: number`, `title: string`, `year: number | null`, `work_type: string`, `relevance: number | null`, `relevance_rank: number | null`, `expansion_priority: number | null`, `expansion_rank: number | null`, `inclusion_status: string`, `is_reference: boolean`, `scored_on: string | null`, `ranking_explanation: string | null`, `reviewed: number`, `user_overrides: string | null // JSON: {relevance?, expansion_priority?, reason?}`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `dossier_briefing_get`

THE BRIEFING: what a model is told about this project before it reads any single paper. Four parts the project already holds — `about` (the project’s own statement of what it is for), `terms` (the definitions written on its attached extraction schemas), `papers` (every paper with its role and how much text it has), `contributions` (the opening paragraph of each paper’s project-scoped summary) — plus `sizes`, the characters each part costs. THIS IS NOT A LIST OF EXTRACTED VALUES: a measurement is quoted to you when you read the paper that reported it, and is not background. `sizes.compiled` is 0 until dossier_build has run, and a 0 there means the one part that reads the chosen papers TOGETHER does not exist yet.

access **read** · channel `dossier:briefing`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`DossierBriefingDTO`

`DossierBriefingDTO` fields: `about: string | null`, `terms: DossierTermGroupDTO[]`, `papers: DossierPaperDTO[]`, `contributions: DossierContributionDTO[]`, `sizes: {`

#### `dossier_build`

Build (or rebuild) a project’s topic dossier from the papers marked as its reference papers, and return the refreshed status. Mark papers with dossier_add_paper FIRST — with none marked this fails with a message saying so. SYNCHRONOUS and slow: it calls the model once per reference paper, serialized, so ten reference papers can run for many minutes and there is NO job to poll. If the call times out the build may STILL BE RUNNING — read dossier_status_get rather than retrying, because a second build started on top of a running one can supersede its results. Each paper’s claims are stored under this project’s id, never globally. A project summary (paper_summary_generate with kind "project") requires this to have run.

access **write** · long-running · channel `dossier:build`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`DossierStatusDTO`

`DossierStatusDTO` fields: `references: DossierSourceDTO[]`, `fallback: boolean`, `sources: DossierSourceDTO[]`, `built_at: string | null`, `built_model: string | null`, `built_prompt_version: string | null`, `built_work_ids: number[]`, `stale: DossierStaleWorkDTO[]`, `current: boolean`

#### `dossier_context_get`

THE BACKGROUND ITSELF, on demand: the slice of a project’s topic dossier that is relevant to ONE paper — the same material the app’s own analyses of that paper were given. CALL THIS whenever a read handed you a project_context saying "already_sent" whose dossier_hash you cannot actually find earlier in your context: a new conversation, a compacted history or an inherited summary all leave you without it, and answering from background you cannot see is guessing. Idempotent, read-only and always safe to call. Returns state "none" when the project has no reference papers to draw background from — an answer, not a failure. projectId 0 is the GLOBAL sentinel and names no project, so it has no dossier. This is BACKGROUND: never let it override a value the paper itself reports, and never quote it as something that paper says.

access **read** · channel `dossier:context`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`DossierContextDTO { state, dossier_hash, note, entries[] }`

`DossierContextDTO` fields: `state: 'ready' | 'none'`, `dossier_hash: string | null`, `note: string | null`, `entries: Array<{`

#### `dossier_get`

The claims in a project’s topic dossier — what its reference papers assert, each with the paper it came from and the quote backing it. `is_contrary: true` marks material that DISAGREES with the rest and exists specifically to stop a reader anchoring on one view: never drop or downweight those when summarising. `analysis_type` of "dossier" means the claim came from a dossier BUILD; any other value means it is a by-product of extracting that one paper. `evidence_verbatim: false` means the model asserted that wording and it could NOT be found in the document — do not quote it as something the paper says. Paginated; read `total`.

access **read** · arguments are capped · channel `dossier:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`DossierEntryDTO[]  (MCP: run yields { items, total }, shaped to a ListScope)`

`DossierEntryDTO` fields: `id: number`, `work_id: number`, `work_title: string`, `work_year: number | null`, `predicate: string`, `subject: string | null`, `value_text: string | null`, `kind: string`, `is_contrary: boolean`, `from_reference: boolean`, `quote: string | null`, `evidence_verbatim: boolean`, `evidence_page: number | null`, `evidence_section: string | null`, `evidence_version: string | null`, `analysis_type: string`, `run_origin: RunOrigin`, `origin_note: string | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `dossier_status_get`

The state of a project’s topic dossier. READ `references` AND `sources` AS TWO DIFFERENT THINGS: `references` are the papers MARKED as sources of the dossier, and `sources` are the papers the current build actually read. A paper you just marked with dossier_add_paper appears in the first and NOT the second until you call dossier_build — that gap is the expected state, not a failure. `stale` lists papers whose analyses have changed since the build, and `built_at`/`built_model`/`built_prompt_version` say what produced the current one. A project with NO reference paper marked has an EMPTY dossier and supplies no background to any analysis: nothing is substituted for it, so emptiness is a fact about the project rather than a gap to work around. (`fallback` is always false; it is kept only for compatibility.)

access **read** · channel `dossier:status`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`DossierStatusDTO`

`DossierStatusDTO` fields: `references: DossierSourceDTO[]`, `fallback: boolean`, `sources: DossierSourceDTO[]`, `built_at: string | null`, `built_model: string | null`, `built_prompt_version: string | null`, `built_work_ids: number[]`, `stale: DossierStaleWorkDTO[]`, `current: boolean`

## Queue and jobs

The background pipeline: what is running, what failed, what is stale.

#### `job_cancel`

Cancel one job. A job that has not started is dropped; a job already running is fenced off so it cannot commit, and the output of THAT STAGE is deleted. Earlier stages’ output and the paper itself are untouched. Returns nothing; poll job_get for the resulting state. If this call times out the cancel is NOT rolled back — read job_get back rather than assuming it did not happen.

access **destructive** · channel `jobs:cancel`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `jobId` | `integer` | yes | ≥ 0 |

**Response**

`void`

#### `job_dismiss`

Mark a job dismissed (or undismissed), which clears it from the failed count without deleting the record or undoing anything it did. Use it to acknowledge a failure the human has decided not to act on — it is their queue, so do not clear failures they have not seen. Returns the project’s refreshed job list, truncated like jobs_list. If this call times out the flag is NOT rolled back; read it back before retrying.

access **write** · channel `jobs:setDismissed`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `jobId` | `integer` | yes | ≥ 0 |
| `dismissed` | `boolean` | yes | — |
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`JobDTO[]`

`JobDTO` fields: `id: number`, `job_type: string`, `stage: string | null`, `fanout_key: string`, `status: string`, `outcome: string | null`, `outcome_note: string | null`, `error_kind: string | null`, `blocked_by: string[]`, `dead_blockers: string[]`, `progress_pct: number | null`, `progress_note: string | null`, `stage_run_duration_ms: number | null`, `stage_run_superseded: boolean | null`, `stage_run_id: number | null`, `work_id: number | null`, `work_title: string | null`, `work_doi: string | null`, `error: string | null`, `attempts: number`, `dismissed: boolean`, `updated_at: string`, `created_at: string`, `started_at: string | null`, `finished_at: string | null`, `document_id: number | null`, `text_source: 'unknown' | 'pdf-text-layer' | 'ocr' | null`, `text_confidence: number | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `job_get`

One job by its id, in the same shape jobs_list returns, or null when no job has that id. Not scoped to a project — a job id is already the answer to "which job", and the row names its own project. This is the cheap way to poll a single asynchronous operation you started.

access **read** · channel `jobs:get`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `jobId` | `integer` | yes | ≥ 0 |

**Response**

`JobDTO | null`

`JobDTO` fields: `id: number`, `job_type: string`, `stage: string | null`, `fanout_key: string`, `status: string`, `outcome: string | null`, `outcome_note: string | null`, `error_kind: string | null`, `blocked_by: string[]`, `dead_blockers: string[]`, `progress_pct: number | null`, `progress_note: string | null`, `stage_run_duration_ms: number | null`, `stage_run_superseded: boolean | null`, `stage_run_id: number | null`, `work_id: number | null`, `work_title: string | null`, `work_doi: string | null`, `error: string | null`, `attempts: number`, `dismissed: boolean`, `updated_at: string`, `created_at: string`, `started_at: string | null`, `finished_at: string | null`, `document_id: number | null`, `text_source: 'unknown' | 'pdf-text-layer' | 'ocr' | null`, `text_confidence: number | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `job_retry`

Re-queue one failed job so it runs again, without re-processing the whole paper. This is ASYNCHRONOUS: it returns nothing and the work happens later — poll job_get for the outcome. Retrying a job whose failure was "needs-user-action" or "upstream" will simply fail the same way — read the job’s error_kind first and fix the cause. If this call times out the re-queue is NOT rolled back; read job_get back before retrying, or you will queue the same work twice.

access **write** · channel `jobs:retry`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `jobId` | `integer` | yes | ≥ 0 |

**Response**

`void`

#### `jobs_list`

The processing jobs for a project, newest activity first. THIS IS THE POLLING TOOL: every asynchronous operation (importing a paper, retrieving references, re-running a stage) returns job ids and finishes here — there is no push notification. Jobs with project_id 0 are global and are always included. "status" says where a job is; "outcome" says how a finished one ended, and they are different questions — a job can be done because it succeeded or done because its precondition was absent. Paginated: pass limit and offset (limit defaults to 200); "total" is every job for the project, so compare it with how many items you received.

access **read** · arguments are capped · channel `jobs:list`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `limit` | `integer \| null` | no | — |
| `offset` | `integer \| null` | no | — |

**Response**

`JobDTO[]`

`JobDTO` fields: `id: number`, `job_type: string`, `stage: string | null`, `fanout_key: string`, `status: string`, `outcome: string | null`, `outcome_note: string | null`, `error_kind: string | null`, `blocked_by: string[]`, `dead_blockers: string[]`, `progress_pct: number | null`, `progress_note: string | null`, `stage_run_duration_ms: number | null`, `stage_run_superseded: boolean | null`, `stage_run_id: number | null`, `work_id: number | null`, `work_title: string | null`, `work_doi: string | null`, `error: string | null`, `attempts: number`, `dismissed: boolean`, `updated_at: string`, `created_at: string`, `started_at: string | null`, `finished_at: string | null`, `document_id: number | null`, `text_source: 'unknown' | 'pdf-text-layer' | 'ocr' | null`, `text_confidence: number | null`

Reshaped over MCP — a list arrives in the [envelope](./mcp.md#what-comes-back).

#### `queue_pause`

Pause the processing queue. Pause means "claim no new work" — a running job is NEVER aborted, so the returned inFlight can stay above 0 after a successful pause, and it will drain on its own. Returns the queue state. Remember to call queue_resume: a queue left paused silently stops everything the human queues afterwards too, and a timed-out call is NOT rolled back — read queue_state back rather than assuming the pause failed.

access **write** · channel `jobs:pause`

**Request**

_No arguments._

**Response**

`{ running: boolean; inFlight: number }`

#### `queue_resume`

Resume the processing queue, so it starts claiming pending work again. Returns the queue state. Safe to call when already running, and a timed-out call is NOT rolled back — read queue_state back rather than calling it twice.

access **write** · channel `jobs:resume`

**Request**

_No arguments._

**Response**

`{ running: boolean; inFlight: number }`

#### `queue_settings_get`

How much work the queue does at once. Two limits, both ABSOLUTE across the whole app rather than per project: "llm" caps the AI steps in flight and "local" caps everything else (reading a PDF, OCR, segmenting, embedding). They are separate because an AI step waits on the gateway and uses no CPU, so counting it against the same allowance as OCR would leave the machine idle while the queue looked full. "is_default" is false once the user has changed either one.

access **read** · channel `jobs:settings`

**Request**

_No arguments._

**Response**

`QueueSettingsDTO`

`QueueSettingsDTO` fields: `llm: number`, `local: number`, `is_default: boolean`

#### `queue_settings_reset`

Forget every queue limit the user chose, so the app returns to the values it ships with. The stored rows are removed rather than rewritten, so a future change to a default reaches this install too.

access **write** · channel `jobs:resetSettings`

**Request**

_No arguments._

**Response**

`QueueSettingsDTO`

`QueueSettingsDTO` fields: `llm: number`, `local: number`, `is_default: boolean`

#### `queue_settings_set`

Change how much work the queue does at once. Omit a field to leave it alone. Raising "llm" costs proportionally more per run and makes a provider rate limit proportionally more likely, which is why it ships at 1. Takes effect on the next scheduling tick — no restart — and never interrupts a step already running: a lowered limit simply stops new work being claimed until the count is back under it.

access **write** · channel `jobs:setSettings`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `llm` | `integer \| null` | no | — |
| `local` | `integer \| null` | no | — |

**Response**

`QueueSettingsDTO`

`QueueSettingsDTO` fields: `llm: number`, `local: number`, `is_default: boolean`

#### `queue_state`

Whether the processing queue is claiming work, and how many jobs are in flight right now. "running: false" with "inFlight" above zero is a normal state, not a contradiction: pausing stops new claims and never aborts a job already underway.

access **read** · channel `jobs:state`

**Request**

_No arguments._

**Response**

`{ running: boolean; inFlight: number }`

#### `stages_list`

The pipeline stages, in the order they actually run, with each stage’s id, version, scope and whether it calls the model. This is the vocabulary for anything that names a stage — read the ids from here rather than guessing them. Stages that use the model are serialized behind one global gate, so such a job can sit queued for a long time with nothing wrong.

access **read** · channel `jobs:stages`

**Request**

_No arguments._

**Response**

`StageDefDTO[]`

`StageDefDTO` fields: `id: string`, `label: string`, `version: string`, `index: number`, `scope: 'document' | 'project' | 'corpus'`, `uses_llm: boolean`

## Export and outlets

Reading what an export or an outlet would produce, and running one.

#### `export_options_list`

What this project can be exported AS — the structural formats plus one option per extraction schema attached to it. Format names come from the database, so read them here rather than assuming any.

access **read** · channel `export:options`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`ExportOptionDTO[]`

`ExportOptionDTO` fields: `id: string`, `group: string`, `format: string`, `label: string`, `description: string`, `extension: string`

#### `outlet_action_run`

Perform an outlet action — a REAL side effect OUTSIDE this app: it writes files into the user’s own Obsidian vault or Zotero library, which nothing here can undo. Some actions overwrite notes the user may have edited by hand, and some delete notes for papers no longer in the project; outlet_actions_list says which by its `writes` flag and its description. Call outlet_actions_list first and do not run an action with a `disabled_reason`. The preconditions are re-checked here, because a vault can be unmounted between listing and running. Never run this speculatively.

access **destructive** · long-running · narrower arguments over MCP than in the app · channel `outlets:run`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `outletId` | `"zotero" \| "obsidian"` | yes | — |
| `actionId` | `"write" \| "overwrite" \| "cleanup"` | yes | — |

**Response**

`{ ok, message, files_written, error }`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `outlet_actions_list`

What an outlet can be asked to do for a project, and whether each action is currently possible. `writes: true` means the action changes files OUTSIDE this app. `disabled_reason` is non-null when the action would refuse and says why. Read this before outlet_action_run — a reason here is a reason the run will fail.

access **read** · channel `outlets:actions`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `outletId` | `"zotero" \| "obsidian"` | yes | — |

**Response**

`OutletActionDTO[]`

`OutletActionDTO` fields: `id: string`, `label: string`, `description: string`, `writes: boolean`, `disabled_reason: string | null`

#### `outlet_note_preview`

The markdown note that WOULD be written for one paper in one project, rendered by the same code that writes the file — so what you see here is what an outlet action would produce. Nothing is written. Null when the paper is not in the project.

access **read** · channel `outlets:previewNote`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |
| `workId` | `integer` | yes | ≥ 0 |

**Response**

`string | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `outlets_list`

The places this project’s work can be mirrored to (Obsidian, Zotero), each with whether it is ready, the checks behind that verdict, and when it last ran. `ready: false` means an action would refuse; the check labels say which precondition failed. Folder locations are reported as configured/not-configured, not as paths.

access **read** · channel `outlets:list`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `projectId` | `integer` | yes | ≥ 0 |

**Response**

`OutletStatusDTO[]`

`OutletStatusDTO` fields: `id: 'zotero' | 'obsidian'`, `name: string`, `tagline: string`, `headline: string`, `ready: boolean`, `checks: OutletCheckDTO[]`, `last_error: string | null`, `last_run_at: string | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

## Install status

Is this connected to the corpus you think it is, and is the model reachable.

#### `integrations_status`

Whether this machine’s Zotero library and Obsidian vault are present and readable by the app. EVERY FLAG IS TRI-STATE and `null` always means the probe did not answer, so the state is genuinely unknown — never read `null` as "no". Locations are reported as configured/not-configured rather than as paths.

access **read** · channel `integrations:status`

**Request**

_No arguments._

**Response**

`IntegrationsStatusDTO`

`IntegrationsStatusDTO` fields: `zotero_installed: boolean | null`, `zotero_running: boolean | null`, `zotero_accessible: boolean | null`, `zotero_data_path: string | null`, `obsidian_enabled: boolean | null`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

#### `llm_status_get`

Which analysis model this app actually resolved, as opposed to which one is preferred. `live: false` means no model can be reached, and in that case NO analysis run is produced at all — check this first when a model-calling tool fails. `reason` is safe to quote and never contains a credential.

access **read** · channel `settings:llmStatus`

**Request**

_No arguments._

**Response**

`LlmStatusDTO`

`LlmStatusDTO` fields: `live: boolean`, `provider: string`, `model: string`, `reason: string`, `token_minutes: number | null`

#### `llm_status_recheck`

Re-run the gateway pre-flight NOW and return the selection it produced. The status above is a snapshot; this is how an outage that has since been fixed is picked up without restarting the app. Safe to call repeatedly — it is one health request, and concurrent calls share the one in flight.

access **read** · channel `settings:llmRecheck`

**Request**

_No arguments._

**Response**

`LlmStatusDTO`

`LlmStatusDTO` fields: `live: boolean`, `provider: string`, `model: string`, `reason: string`, `token_minutes: number | null`

#### `model_list`

The models the gateway currently serves. Read from the gateway’s own health report at startup, so it is what this app can actually reach rather than a list someone typed. Empty when no gateway answered, which is the honest answer — not a guess at what might be available.

access **read** · channel `llm:models`

**Request**

_No arguments._

**Response**

`string[]`

#### `model_settings_get`

Which model each kind of work uses, and how much room it is given. TWO ROLES, kept separate on purpose: "extraction" reads every paper and is the volume cost, while "review" reads a table a SECOND time to disagree with the first reading — its whole value is that it fails differently, so running it on the extractor’s model would make it confirm rather than check. An empty extractionModel means the gateway chooses, which is what this app has always done. The context window is REPORTED, never enforced: a paper is split across messages rather than trimmed to fit one.

access **read** · channel `llm:settings`

**Request**

_No arguments._

**Response**

`ModelSettingsDTO`

`ModelSettingsDTO` fields: `extractionModel: string`, `extractionMaxOutput: number`, `extractionContext: number`, `reviewModel: string`, `reviewMaxOutput: number`, `reviewContext: number`, `is_default: boolean`

#### `model_settings_reset`

Forget every model choice, so the app returns to what it ships with. The stored rows are removed rather than rewritten, so a later change to a default reaches this install too.

access **write** · channel `llm:resetSettings`

**Request**

_No arguments._

**Response**

`ModelSettingsDTO`

`ModelSettingsDTO` fields: `extractionModel: string`, `extractionMaxOutput: number`, `extractionContext: number`, `reviewModel: string`, `reviewMaxOutput: number`, `reviewContext: number`, `is_default: boolean`

#### `model_settings_set`

Change which model a kind of work uses, or how much room it has. Omit a field to leave it alone. A model named here must be one the gateway serves — model_list reports them — and a request it cannot honour is refused by the gateway with an error naming the model rather than silently downgraded. Takes effect on the next stage that runs; work already in flight finishes under the setting it started with.

access **write** · channel `llm:setSettings`

**Request**

| field | type | required | notes |
|---|---|---|---|
| `extractionModel` | `string \| null` | no | — |
| `reviewModel` | `string \| null` | no | — |
| `extractionMaxOutput` | `integer \| null` | no | — |
| `reviewMaxOutput` | `integer \| null` | no | — |
| `extractionContext` | `integer \| null` | no | — |
| `reviewContext` | `integer \| null` | no | — |

**Response**

`ModelSettingsDTO`

`ModelSettingsDTO` fields: `extractionModel: string`, `extractionMaxOutput: number`, `extractionContext: number`, `reviewModel: string`, `reviewMaxOutput: number`, `reviewContext: number`, `is_default: boolean`

#### `storage_usage_get`

How much disk each project’s stored PDFs and text occupy, in bytes. Byte counts only — no locations. Each project also lists its LARGEST papers, the ten that would free the most space, with "papers_total" saying how many it has in all. Read it before importing in bulk.

access **read** · channel `settings:storageUsage`

**Request**

_No arguments._

**Response**

`StorageProjectDTO[]  (MCP: papers truncated, plus papers_total/papers_truncated)`

`StorageProjectDTO` fields: `project_id: number`, `name: string`, `size_bytes: number`, `papers: StoragePaperDTO[]`

Reshaped over MCP — this is NOT the list envelope; it is trimmed or redacted in place, and has no `total`/`offset`.

## Not exposed as tools

These capabilities exist in the app and deliberately have no tool. Ask the person
using the app to do them.

| channel | why |
|---|---|
| `projects:create` | Create a project. Not exposed as a tool: a project is the frame a scientist states their own question in, and an agent inventing one would put every later interpretation under a question nobody asked. |
| `projects:updateSetup` | Write one answer from the creation questionnaire. Not exposed as a tool for the same reason creating a project is not: what a project is for is the scientist’s own statement, and an agent editing it would restate the question every later interpretation is read against. |
| `projects:finishSetup` | Finish a project’s setup: make every paper imported during it a reference paper, build the project context from them, and mark the project ready. Not a tool: it completes a form only a person fills in. |
| `schemas:create` | Define a new, empty extraction schema by name. The schema’s key is derived from the name and its version is hashed from its fields, so neither can be supplied. A schema is a statement about what matters in this literature — do not invent one that was not asked for. Returns the created schema. |
| `schemas:update` | Rename an extraction schema or change its description. Does not touch its fields or any value already extracted under it. Returns the updated schema. |
| `schemas:addField` | Add one field to an extraction schema: what it is called, its data type (number/text/enum/boolean), its unit, whether it is required, and for an enum its allowed values. Adding a field changes the schema’s version, so papers extracted under the older version are marked stale rather than silently treated as current. Returns the updated schema. |
| `schemas:updateField` | Change one field of an extraction schema — its label, type, unit, requiredness, allowed values or position. Changes the schema’s version, so already-extracted papers are marked stale rather than reinterpreted under the new definition. Returns the updated schema. |
| `schemas:import` | Create a new extraction schema from a bundle (one from schema_presets_list, or one schema_export produced elsewhere). ALWAYS CREATES; it never overwrites or merges into an existing schema. A bundle written by a newer version of the app is refused with a message saying so rather than partially read. Returns the created schema. |
| `extraction:crops` | The table pictures a paper’s extraction was read off, rendered by the same code path that put them in the extractor’s prompt. |
| `outlets:getSettings` | The outlets’ stored settings. Not exposed as a tool: its payload IS the vault path and the Zotero data directory, which would hand an agent the user’s home directory and OS account name. outlets_list reports the same readiness without them. |
| `outlets:updateSettings` | Change an outlet’s settings. Permanently not a tool: the Obsidian settings carry `vault_path`, so an agent that could set it and then run an outlet action would have arbitrary file write as the user. A human chooses this folder, in the app. |
| `export:project` | Serialize a whole project to a string in one of the formats export_options_list reports. |
| `ingest:attachPdfPath` | Attach a PDF on this machine, named by absolute path, to a paper the library already holds. The UI’s drop-onto-a-failed-row path; agents use paper_attach_pdf. |
| `tabs:state` | The calling window's own tabs and which one is active. |
| `tabs:open` | Open a page as a tab in the calling window, or focus the tab that already shows it. Dedupe is per WINDOW: a sibling window holding the same page is reported, not focused. |
| `tabs:activate` | Make one of the calling window’s tabs the active one. |
| `tabs:close` | Close one of the calling window’s tabs. Refused for the LAST tab — a window always shows something. |
| `tabs:reorder` | Set the calling window’s tab order. Accepts only a permutation of the keys it already holds, so a drag that ends badly cannot drop a tab. |
| `tabs:detach` | Drag a tab out into a NEW window, positioned at the cursor. The tab stays here, marked, until the new window claims it, so a window that fails to open reverts the move instead of destroying the page. |
| `tabs:adopt` | Claim the tab this window was created to receive. Takes no key: main knows what it promised, so a window cannot claim a page merely by naming it. |
| `tabs:setRoute` | Move a tab to a different page in place, after a navigation inside it. Does not bump the rev — the tab still exists, it is just showing something else — but main must be told, because main answers “is this page already open”. |
| `tabs:setTitle` | Rename a tab once its subject has loaded. Does not bump the rev — a title changes what the strip draws, not which tabs exist. |
| `tabs:setViewState` | Store a tab’s opaque view snapshot (scroll, find query, sub-tab), so it can be restored after a suspend or carried to another window. |
